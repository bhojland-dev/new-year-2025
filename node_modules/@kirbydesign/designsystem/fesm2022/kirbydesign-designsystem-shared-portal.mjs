import { CdkPortalOutlet, DomPortal } from '@angular/cdk/portal';
import * as i0 from '@angular/core';
import { Input, Directive } from '@angular/core';

/**
 * @summary PortalDirective is a utility that lets you portal the host element into another DOM element.
 * This is designed to be beneficial when there's issues with the stacking context.
 *
 * Extends CdkPortalOutlet to gain functionality for attaching/detaching the host element, and for proper cleanup.
 *
 * Using portal directive to reposition HTML elements, might break angular functionality and/or styling, so use
 * with care.
 */
class PortalDirective extends CdkPortalOutlet {
    /** The DOM element for which the host element should be appended as a child. */
    set outlet(outlet) {
        this._outlet = outlet;
        if (this.outlet) {
            this.portal = new DomPortal(this.elementRef);
        }
    }
    get outlet() {
        return this._outlet;
    }
    /**
     * Override portal to handle if user provides a portal of another type than DOMPortal, which CdkPortal would
     * otherwise allow. This can be modified to handle other portal types in the future, should the need arise
     */
    set portal(portal) {
        const isDOMPortal = portal instanceof DomPortal;
        if (!isDOMPortal) {
            throw Error(`Portal type ${portal} is not supported`);
        }
        super.portal = portal;
    }
    constructor(elementRef, componentFactoryResolver, viewContainerRef) {
        super(componentFactoryResolver, viewContainerRef);
        this.elementRef = elementRef;
        this.componentFactoryResolver = componentFactoryResolver;
        this.viewContainerRef = viewContainerRef;
        /**
         * Nearly identical to super class, yet we want to use provided outlet instead of directive host as new content
         * parent which is otherwise the default for CdKPortal
         */
        this.attachDomPortal = (portal) => {
            if (!this.outlet) {
                throw Error('Outlet must be defined!');
            }
            const { element } = portal;
            if (!element.parentNode) {
                throw Error('DOM portal content must be attached to a parent node.');
            }
            // Anchor used to save the element's previous position so
            // that we can restore it when the portal is detached.
            const anchorNode = document.createComment('dom-portal');
            portal.setAttachedHost(this);
            element.parentNode.insertBefore(anchorNode, element);
            this.outlet.appendChild(element);
            this._attachedPortal = portal;
            super.setDisposeFn(() => {
                if (anchorNode.parentNode) {
                    anchorNode.parentNode.replaceChild(element, anchorNode);
                }
            });
        };
    }
    /** @nocollapse */ static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: PortalDirective, deps: [{ token: i0.ElementRef }, { token: i0.ComponentFactoryResolver }, { token: i0.ViewContainerRef }], target: i0.ɵɵFactoryTarget.Directive }); }
    /** @nocollapse */ static { this.ɵdir = i0.ɵɵngDeclareDirective({ minVersion: "14.0.0", version: "19.2.14", type: PortalDirective, isStandalone: true, selector: "[kirbyPortal]", inputs: { outlet: "outlet" }, usesInheritance: true, ngImport: i0 }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: PortalDirective, decorators: [{
            type: Directive,
            args: [{
                    selector: '[kirbyPortal]',
                    standalone: true,
                }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.ComponentFactoryResolver }, { type: i0.ViewContainerRef }], propDecorators: { outlet: [{
                type: Input
            }] } });

/**
 * Generated bundle index. Do not edit.
 */

export { PortalDirective };
//# sourceMappingURL=kirbydesign-designsystem-shared-portal.mjs.map
