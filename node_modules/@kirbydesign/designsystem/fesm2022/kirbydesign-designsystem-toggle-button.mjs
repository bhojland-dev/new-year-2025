import * as i0 from '@angular/core';
import { EventEmitter, forwardRef, HostListener, Output, Input, ChangeDetectionStrategy, Component, NgModule } from '@angular/core';
import { NG_VALUE_ACCESSOR } from '@angular/forms';
import * as i1 from '@angular/common';
import { CommonModule } from '@angular/common';

class ToggleButtonComponent {
    constructor(elementRef, cdr) {
        this.elementRef = elementRef;
        this.cdr = cdr;
        this.checkChanged = new EventEmitter();
        // eslint-disable-next-line @typescript-eslint/no-unused-vars, no-empty-function
        this.onChange = (_checked) => { };
        // eslint-disable-next-line no-empty-function, @typescript-eslint/no-empty-function
        this.onTouched = () => { };
    }
    onClick(event) {
        const targetElement = event.target;
        const buttonEnabled = targetElement.closest('button[kirby-button]:not(:disabled)');
        if (!buttonEnabled)
            return;
        this.checked = !this.checked;
        this.onChange(this.checked);
        this.onTouched();
        this.checkChanged.emit(this.checked);
        this.focusToggledButton();
    }
    onFocusOut() {
        this.onTouched();
    }
    focusToggledButton() {
        // force re-render to ensure that the new button is in the dom
        this.cdr.detectChanges();
        const buttonToFocus = this.elementRef.nativeElement.querySelector('button[kirby-button]');
        buttonToFocus?.focus();
    }
    /**
     * Sets the toggle button's value. Part of the ControlValueAccessor interface
     * required to integrate with Angular's core forms API.
     *
     * @param checked New value to be written to the model.
     */
    writeValue(checked) {
        if (this.checked !== checked) {
            this.checked = checked;
            this.cdr.markForCheck();
        }
    }
    /**
     * Saves a callback function to be invoked when the toggle button's value
     * changes from user input. Part of the ControlValueAccessor interface
     * required to integrate with Angular's core forms API.
     *
     * @param fn Callback to be triggered when the value changes.
     */
    registerOnChange(fn) {
        this.onChange = fn;
    }
    /**
     * Saves a callback function to be invoked when the toggle button is blurred
     * by the user. Part of the ControlValueAccessor interface required
     * to integrate with Angular's core forms API.
     *
     * @param fn Callback to be triggered when the component has been touched.
     */
    registerOnTouched(fn) {
        this.onTouched = fn;
    }
    /**
     * Disables the toggle button. Part of the ControlValueAccessor interface required
     * to integrate with Angular's core forms API.
     *
     * @param isDisabled Sets whether the component is disabled.
     */
    setDisabledState(isDisabled) {
        this.elementRef.nativeElement
            .querySelector('button[kirby-button]')
            ?.toggleAttribute('disabled', isDisabled);
    }
    /** @nocollapse */ static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonComponent, deps: [{ token: i0.ElementRef }, { token: i0.ChangeDetectorRef }], target: i0.ɵɵFactoryTarget.Component }); }
    /** @nocollapse */ static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "19.2.14", type: ToggleButtonComponent, isStandalone: false, selector: "kirby-toggle-button", inputs: { checked: "checked" }, outputs: { checkChanged: "checkChanged" }, host: { listeners: { "click": "onClick($event)", "focusout": "onFocusOut()" } }, providers: [
            {
                provide: NG_VALUE_ACCESSOR,
                useExisting: forwardRef((() => ToggleButtonComponent)),
                multi: true,
            },
        ], ngImport: i0, template: "<ng-content *ngIf=\"!checked\" select=\"button[kirby-button][unchecked]\"></ng-content>\n<ng-content *ngIf=\"checked\" select=\"button[kirby-button][checked]\"></ng-content>\n", dependencies: [{ kind: "directive", type: i1.NgIf, selector: "[ngIf]", inputs: ["ngIf", "ngIfThen", "ngIfElse"] }], changeDetection: i0.ChangeDetectionStrategy.OnPush }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonComponent, decorators: [{
            type: Component,
            args: [{ selector: 'kirby-toggle-button', changeDetection: ChangeDetectionStrategy.OnPush, standalone: false, providers: [
                        {
                            provide: NG_VALUE_ACCESSOR,
                            useExisting: forwardRef((() => ToggleButtonComponent)),
                            multi: true,
                        },
                    ], template: "<ng-content *ngIf=\"!checked\" select=\"button[kirby-button][unchecked]\"></ng-content>\n<ng-content *ngIf=\"checked\" select=\"button[kirby-button][checked]\"></ng-content>\n" }]
        }], ctorParameters: () => [{ type: i0.ElementRef }, { type: i0.ChangeDetectorRef }], propDecorators: { checked: [{
                type: Input
            }], checkChanged: [{
                type: Output
            }], onClick: [{
                type: HostListener,
                args: ['click', ['$event']]
            }], onFocusOut: [{
                type: HostListener,
                args: ['focusout']
            }] } });

const DECLARATIONS = [ToggleButtonComponent];
class ToggleButtonModule {
    /** @nocollapse */ static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    /** @nocollapse */ static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonModule, declarations: [ToggleButtonComponent], imports: [CommonModule], exports: [ToggleButtonComponent] }); }
    /** @nocollapse */ static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonModule, imports: [CommonModule] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "19.2.14", ngImport: i0, type: ToggleButtonModule, decorators: [{
            type: NgModule,
            args: [{
                    declarations: [...DECLARATIONS],
                    imports: [CommonModule],
                    exports: [...DECLARATIONS],
                }]
        }] });

/**
 * Generated bundle index. Do not edit.
 */

export { ToggleButtonComponent, ToggleButtonModule };
//# sourceMappingURL=kirbydesign-designsystem-toggle-button.mjs.map
