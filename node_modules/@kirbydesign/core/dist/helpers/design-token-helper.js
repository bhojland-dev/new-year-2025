import { ColorHelper } from './color-helper';
import { styles } from './design-token-helper.styles';
export class DesignTokenHelper {
    static { this.breakpoints = styles.breakpoints; }
    static { this.softKeyboardTransitionEnter = styles.softKeyboardTransitionEnter; }
    static { this.softKeyboardTransitionLeave = styles.softKeyboardTransitionLeave; }
    static { this.modalDefaultHeight = styles.modalDefaultHeight; }
    static { this.drawerDefaultHeight = styles.drawerDefaultHeight; }
    static getColor(name, variant) {
        const variantSuffix = variant ? `-${variant}` : '';
        const colorVariant = `${name}${variantSuffix}`;
        return {
            name: name,
            variant: variant,
            fullname: colorVariant,
            value: ColorHelper.getThemeColorRgbString(colorVariant),
            hex: ColorHelper.getThemeColorHexString(colorVariant),
        };
    }
    static getDecorationColor(name, step) {
        return {
            name: name,
            step: step,
            fullname: `${name}-${step}`,
            value: ColorHelper.getThemeDecorationColorRgbString(name, step),
            hex: ColorHelper.getThemeDecorationColorHexString(name, step),
        };
    }
    static getTextColor(name, variant) {
        const variantSuffix = variant ? `-${variant}` : '';
        const colorVariant = `${name}${variantSuffix}`;
        return {
            name: name,
            variant: variant,
            fullname: colorVariant,
            value: ColorHelper.getThemeTextColorRgbString(colorVariant),
            hex: ColorHelper.getThemeTextColorHexString(colorVariant),
        };
    }
    static size(key) {
        return styles.sizes[key];
    }
    static fontSize(key) {
        return styles.fontSizes[key];
    }
    static iconFontSize(key) {
        return styles.iconFontSizes[key];
    }
    static fontWeight(key) {
        return styles.fontWeight[key];
    }
    static lineHeight(key) {
        return styles.lineHeight[key];
    }
    static backgroundColor() {
        return ColorHelper.getBackgroundColor();
    }
    static borderRadius(key = undefined) {
        if (key === undefined) {
            console.warn("Calling the borderRadius function without a parameter is deprecated. Please use `borderRadius('n')` instead.");
        }
        return styles.borderRadii[key];
    }
    /**
     * @deprecated The borderRadiusRound function is deprecated.
     * Please use `borderRadius('pill')` instead.
     */
    static borderRadiusRound() {
        console.warn("The borderRadiusRound function is deprecated. Please use `borderRadius('pill')` instead.");
        return DesignTokenHelper.borderRadius('pill');
    }
    static alertMaxWidth() {
        return styles.alertMaxWidth;
    }
    static compactModalMaxWidth() {
        return styles.compactModalMaxWidth;
    }
    static dropdownItemHeight() {
        return styles.dropdownItemHeight;
    }
    static avatarSize(key) {
        return styles.avatarSizes[key];
    }
    static fatFingerSize() {
        return styles.fatFingerSize;
    }
    static getElevation(z) {
        return styles.elevations[z];
    }
    static itemHeight(key) {
        return styles.itemHeights[key];
    }
    static transitionDuration(key) {
        return styles.transitionDurations[key];
    }
    static transitionEasings() {
        return styles.transitionEasings;
    }
    static zLayer(key) {
        return styles.zLayers[key];
    }
    static pageContentMaxWidth(key) {
        return styles.pageContentMaxWidths[key];
    }
}
