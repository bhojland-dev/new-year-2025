var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
import { html } from 'lit';
import { property } from 'lit/decorators.js';
import KirbyElement from '../internal/kirby-element';
import baseStyles from '../styles/element.base.styles';
import styles from './badge.element.styles';
export class KirbyBadgeElement extends KirbyElement {
    constructor() {
        super(...arguments);
        /** The badge's theme variant. */
        this.themeColor = 'white';
        /** The badge's size. */
        this.size = 'md';
    }
    static { this.tagName = 'kirby-badge'; }
    static { this.styles = [baseStyles, styles]; }
    get text() {
        return this._text;
    }
    /**
     * @deprecated The 'text' property has been deprecated.
     * Please set the text of the Badge between the opening and closing tag.
     */
    set text(value) {
        console.warn('[Kirby Badge] The `text` property has been deprecated. Please set the text of the Badge between the opening and closing tag.');
        this._text = value;
    }
    render() {
        const content = this.text && this.size === 'md'
            ? html `
            ${this.text}
          `
            : html `
            <slot ?hidden=${this.size === 'sm'}></slot>
          `;
        return html `
      <span part="base" role="status">${content}</span>
    `;
    }
}
__decorate([
    property({ reflect: true }),
    __metadata("design:type", String)
], KirbyBadgeElement.prototype, "themeColor", void 0);
__decorate([
    property({ reflect: true }),
    __metadata("design:type", String)
], KirbyBadgeElement.prototype, "size", void 0);
__decorate([
    property(),
    __metadata("design:type", String),
    __metadata("design:paramtypes", [String])
], KirbyBadgeElement.prototype, "text", null);
